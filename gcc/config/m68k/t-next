# libgcc1.c is not needed, since the standard library has these functions.
LIBGCC1=

# Specify other dirs of system header files to be fixed.
# It might be ok to leave out /NextDeveloper/Headers,
# but I don't know and can't find out before 2.3.2 comes out.
OTHER_FIXINCLUDES_DIRS= /LocalDeveloper/Headers /NextDeveloper/Headers
