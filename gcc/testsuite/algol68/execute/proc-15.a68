# { dg-options "-fstropping=upper" }  #
# Nested procedures.  #
BEGIN PROC foo = (INT i) INT:
      BEGIN PROC bar = (INT i) INT: i - 1;
            bar (i) * 10
      END;
      ASSERT (foo (10) = 90)
END
