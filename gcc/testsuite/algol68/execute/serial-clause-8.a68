# { dg-options "-fstropping=upper" }  #
BEGIN ASSERT ((INT y := 10;
               INT x := 20;
               REF INT yy;
               (REF INT xx := x;
                yy := y;
                xx
               )
              ) = 20)
END
