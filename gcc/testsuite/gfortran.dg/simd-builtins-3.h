!GCC$ builtin (sin) attributes simd (inbranch)
!GCC$  builtin ( sin) attributes simd (inbranch)
!GCC$ builtin (sin  ) attributes simd (inbranch)
!GCC$ builtin (sin) attributes simd ( inbranch)
!GCC$ builtin (sin) attributes simd (inbranch )
!GCC$ builtin(sin  )   attributes   simd ( inbranch )
!GCC$ builtin  (    sin  )   attributes   simd (   inbranch )
!GCC$ builtin  (    sin  )   attributes   simd
