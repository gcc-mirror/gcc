/* { dg-do compile } */

void
xl (int co)
{
  int ar = 0;

  if (ar != 0)
    {
      int pp, ll = 0;
      int *zx;

      if (co == 0)
	{
	}
      else
	{
	  zx = &pp;

	  if (co < 0)
	    while (co < 1)
	      {
to:
		while (ll < 1)
		  ++ll;
	      }

	  *zx = (__INTPTR_TYPE__)&ar;
	}
    }

  goto to;
}
